/**
 * creator:
 * date:
 * description:
 */
import $log from '@lib/log';
const log = $log.getInstance('app.Member');

export default (async function (ctx) {
  log.info('--- api main update');

  // do somthing
});
